[package]
name = "delta_rust"
version = "0.7.0"
edition = "2024"

[dependencies]
macroquad = "0.4.14"
rodio = "0.20"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
time = { version = "0.3", features = ["formatting", "parsing"] }
